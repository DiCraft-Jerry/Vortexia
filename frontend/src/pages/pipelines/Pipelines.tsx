import { Typography } from 'antd';

const { Title } = Typography;

export default function Pipelines() {
  return (
    <div>
      <Title level={2}>流水线管理</Title>
      <p>流水线管理功能正在开发中...</p>
    </div>
  );
} 